[스트림 (Stream)]
-자바 8 소개
-컬렉션이나 배열의 저장 요소를 하나씩 참조해서 처리할 수 있는 반복자

    *반복자 (Iterator)
    :자바 7 이전까지는 Iterator 반복자 사용
    :자바 8 이후 Stream 사용
    :컬렉션 (java.util.Collection)의
    1) stream() 메소드를 통해 스트림 객체를 얻음
    2) stream.forEach() 를 통해 컬렉션 요소를 하나씩 출력

    *스트림 특정
    1)내부에 있는 반복자를 이용
    :컬렉션 내부에서 요소를 반복시킴
    :개발자는 요소 처리 코드에만 집중할수 있음
    :섹시함
    :병렬 작업을 수월하게 해줌!

    *외부 반복자
    :개발자가 직접 컬렉션 요소를 반복해서 가지고 오는 패턴
    (while,for문을 돌면서 외부 반복문을 이용)

    2)람다식이랑 사용
    3)중단처리 부분 + 최종처리하는 부분
    :중간 처리: 필터링(필요한 요소만 가지고옴),정렬
    :최종 처리 : 원소들의 평균,총합,요소갯수 카운팅 (집계 처리)

    *스트림 종류
    -java.util.stream 패키지는 스트림 API (자바에서 제공하는 함수가 모여있는곳 라이브어리) 존재

    -BaseStream 인터페이스를 부모로 해서 자식 인터페이스들 존재
    인터페이스 와 상속은 클래스가 안되나 인터페이스 끼리는 상속이 가능하다!

       부모        자식
    BaseStream >Stream
               >IntStream
               >DoubleStream


